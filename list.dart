// list is same as array
void main() {
  var myList = [1, 2, 36, 8];
  print(myList);
  // To add a single value in array
  myList.add(20);
  print(myList);
//   To add multiple value in array
  myList.addAll([18, 50, 29]);
  print(myList);
}
